## 1. Refresh reference checkouts

- [x] 1.1 Move `_ref/ProjectTitle` to tag `2026.07-v3.8.3` and confirm `_meta.lua` no longer carries a `name` field — confirmed, `name` field replaced by a deprecation comment
- [x] 1.2 Move `_ref/koreader` to tag `v2026.07.1` and confirm `Version:getNormalizedVersion` still uses the `((year*100+month)*1000000) + point*10000 + revision` formula the pin value depends on — confirmed; `v2026.07.1` and `v2026.07.2` are the same commit `9192014`
- [x] 1.3 Update `scripts/setup-refs.sh` if it pins the old tags, so a fresh clone lands on the new target — it pinned nothing and tracked default branch tips; now derives every ref from the manifest (`2a22cd7`)

## 2. Bump the manifest

- [x] 2.1 Set `plugins/manifest.yml` to `koreader: "2026.07.1"`, `codename: Sailing Walrus`, PT `tag: "2026.07-v3.8.3"`; leave bookends at `v5.20.0` (`2f9bc1c`)
- [x] 2.2 Run the manifest validation in `.github/scripts/` and confirm it still passes — `parse-manifest.sh` OK, test suite 8/8
- [x] 2.3 Added by amendment, after 2.1 deliberately left bookends alone: bump bookends to `v5.22.0` (`43bbcfa`). Verified on its own terms rather than on this change's evidence, by diffing the hooked function bodies between tags: `Bookends:resolveLineConfig` (`main.lua:1019`) and `Bookends:showFontPicker` (`main.lua:2388`) are byte-identical, `Bookends:markDirty` identical, and `bookends_utils.lua` is untouched by the release, so `Utils.resolveFontFace` still resolves. `Bookends:buildBookendsSettingsMenu` (`menu/main_menu.lua:224`) DID change (upstream added a "Disable automatic clock/battery refresh" entry and a `closeMenuInstance` helper), but `2-bookend-unifont.lua:536` locates its insertion point by matching the `Default font` text prefix rather than by index, and that entry survives at `main_menu.lua:264,274`; it also has an explicit append fallback. In `bookends_line_editor.lua` the `line_face` upvalue and its `-- nil = use default` contract are still at line 78, while the `showFontPicker` call site moved 698 -> 700; both stale line references in `2-bookend-line-font-inherit.lua`'s header were corrected. Bundle rebuilt from an empty directory ships `version = "5.22.0"`. Not verified: v5.22.0's large `bookends_tokens.lua` (+251) and `bookends_overlay_widget.lua` changes, which neither patch touches.

## 3. Mechanical registration pass

- [x] 3.1 Change `"coverbrowser"` to `"projecttitle"` in `registerPatchPluginFunc` across the 13 PT patches: `2--disable-all-PT-widgets.lua:134`, `2-new-progress-bar.lua:108`, `2-new-status-icons.lua:116`, `2-pages-badge.lua:111`, `2-percent-badge.lua:108`, `2-pt-footer-history-recent.lua:169`, `2-pt-mm-noborders.lua:34`, `2-pt-perf.lua:597`, `2-rounded-folder-covers.lua:934`, `2-series-badge-numbered.lua:140`, `2-series-indicator.lua:78`, `20-faded-finished-books.lua:54`, `20-rounded-covers.lua:125`
- [x] 3.2 Confirm `grep -rn '"coverbrowser"' patches/` returns no `registerPatchPluginFunc` hits, and that the 2 bookends and 2 coverimage patches were left untouched — key census 13 projecttitle / 2 bookends / 2 coverimage (`e24797f`)
- [~] 3.3 Rename local patch functions named `patchCoverBrowser` — SKIPPED by decision: 6 files of purely cosmetic churn, file-local names with no contract. Revisit only on request.

## 4. Add version pins to all 19 patches

- [x] 4.1 Add `KOReader 2026.07.1 (safe_version 202607010000)` to the header block of every patch in `patches/` (`a3dc216`)
- [~] 4.2 Add `local safe_version = 202607010000` to each patch — SKIPPED by decision: the repo had no such literal (the "existing convention" was comment-only prose), so 19 declarations would be dead code with `unused-local` diagnostics. Header-only form chosen instead; `design.md` still describes the literal and needs correcting.
- [x] 4.3 Re-run the audit against `2026.07.1` and confirm 19 OK, 0 STALE, 0 UNKNOWN — 19/19 pinned, `luajit` parses all 19

## 5. Tier 3 verification (symbol confirmed present)

Both items verified static-only against `_ref/` at `2026.07-v3.8.3` / `v2026.07.1`. No patch edits were required.

- [x] 5.1 `2-pt-mm-noborders.lua` — confirm `ptutil.thinWhiteLine`, `thinGrayLine`, `mediumBlackLine` exist at v3.8.3 and the call signatures are unchanged — VERIFIED, no change needed. All three at `ptutil.lua:542-545`, `ptutil.line` at 531, and `git diff 2026.03-v3.7 2026.07-v3.8.3 -- ptutil.lua` shows zero lines touching any of them. All 8 call sites (`mosaicmenu.lua:1294,1354,1357,1369`, `covermenu.lua:727`, `listmenu.lua:1331,1352,1354`) still resolve through the `ptutil` table at call time, so the override takes effect. `ptutil.thinBlackLine` is used at 3 sites and is not overridden by the patch, but that was equally true at v3.7 (identical line numbers in both tags), so it is pre-existing scope, not a regression.
- [x] 5.2 `2-pt-footer-history-recent.lua` — confirm `CoverMenu.menuInit` is unchanged at v3.8.3 and the `BookInfoManager` setting accessors it uses still resolve — VERIFIED, no change needed. `covermenu.lua` drifted by only 5 insertions / 2 deletions across the whole file, none of them inside `CoverMenu:menuInit` (`covermenu.lua:621`). Confirmed still present and unchanged: the `Menu.init = CoverMenu.menuInit` redirect (`main.lua:1045`) the patch has to shadow; the `page_info` / `cur_folder_text` / `screen_w` fields it mutates; `getSetting` / `saveSetting` / `toggleSetting` (`bookinfomanager.lua:296,303,336`); the `replace_footer_text` and `reverse_footer` keys; the menu path `filemanager_display_mode` -> `Advanced settings` (`main.lua:591`) -> `Footer` (722) -> `Replace folder name with device info` (725), with zero diff on those strings; `ProjectTitle:addToMainMenu` (389); `require("l10n.gettext")`. Host names resolve: `filemanager` from `covermenu.lua:391`, `history` and `collections` from KOReader core (`filemanagerhistory.lua:62`, `filemanagercollection.lua:83`). The `history` and `last_document` icons are NOT in KOReader's `resources/icons/mdlight/`; they arrive via `ptutil.installIcons` (`ptutil.lua:186`), whose list still includes both.

## 6. Tier 2 verification (hooked body changed upstream)

All items verified static-only against `_ref/` at `2026.07-v3.8.3` / `v2026.07.1`.
No patch edits were required. The "rewritten `paintTo`" premise this group was
built on overstated the drift: `git diff 2026.03-v3.7 2026.07-v3.8.3 -- mosaicmenu.lua`
is only two hunks (line 798 and lines 929-966), `listmenu.lua` has zero diff, and
the +666/-133 figure that motivated Tier 2 was `main.lua` plus `ptutil.lua`, which
grew by addition rather than by rewriting what we hook.

- [x] 6.1 Read the rewritten `MosaicMenuItem:paintTo` body at v3.8.3 and record where a wrapper can still hook without losing paint order — DONE. `paintTo` (`mosaicmenu.lua:894`) has one changed region: the series-widget block, now wrapped in `if series_index ~= nil` and sourcing its label from `ptutil.formatSeriesIndex` instead of inline padding. Signature, `InputContainer.paintTo` super call, cover-target lookup `self[1][1][1]`, corner-mark block, progress-bar block, and status-widget block are all unchanged and in the same order. Every wrapper hooks by replacing `MosaicMenuItem.paintTo` and calling the saved original first, so paint order is preserved: the original still paints everything it used to, then the wrapper overlays.
- [x] 6.2 Verify the 8 `paintTo` wrappers against that finding — VERIFIED, no changes needed. All 8 reach `MosaicMenuItem` through `userpatch.getUpValue(MosaicMenu._updateItemsBuildUI, "MosaicMenuItem")`; `_updateItemsBuildUI` is still at `mosaicmenu.lua:1289` and still references `MosaicMenuItem` (declared at 399), so the upvalue resolves. `2-new-progress-bar` additionally takes `getUpValue(paintTo, "corner_mark_size")`; that file-local is declared at `mosaicmenu.lua:50` and still referenced inside `paintTo`, so it is still an upvalue. Every `self.*` field the wrappers read is still populated: `been_opened`, `do_hint_opened`, `height`, `width`, `status`, `percent_finished`, `show_progress_bar`, `is_directory`, `filepath`, `menu`, `pages` (all read by upstream `paintTo` itself); `text`, `dimen`, `menu`, `do_cover_image` (set at construction in `_updateItemsBuildUI`); `file_deleted` (set in `update`). `2--disable-all-PT-widgets` filters on `ImageWidget.file` rather than a menu-item field, matching the four PT status SVGs `trophy` / `pause` / `new` / `large_book`; all four still exist in `resources/` and are still the complete set referenced by `mosaicmenu.lua`, so nothing new escapes the filter.
- [x] 6.3 `2-series-badge-numbered.lua` — check its `MosaicMenuItem:init` and `:free` overrides against the touched `MosaicMenuItem:update` — VERIFIED, no change needed. `update` (`mosaicmenu.lua:462`) was touched at exactly one line (798), dropping the `series_index ~= 0` suppression from the title/author metadata append. Our `init` override calls the original first, so `self.filepath` and the `update` pass have both completed before we read `getBookInfo`; we then read `bookinfo.series_index`, which line 798 does not mutate. Line 798 does mutate `bookinfo.series` in place, and now does so for index 0 as well, but no patch of ours depends on that field's contents. `MosaicMenuItem:free` is still not defined on the class at v3.8.3, so `orig_MosaicMenuItem_free` resolves to the inherited container method and the guarded override behaves exactly as it did at v3.7.
- [x] 6.4 Check the series patches against upstream's new `ptutil` series helpers — VERIFIED, no change needed, and this task's premise was WRONG. v3.8.3 did not remove its series-formatting preferences: `series_mode` has zero diff lines across the whole release, and `main.lua:691` still offers the same toggle. What 3.8 actually did was ADD custom sort collates (`ProjectTitle.addSortMethods`, `BookList.collates.series` / `.pages`, gated on a new `use_custom_sorts` setting), which is why `getSeries` / `zeroPadIndex` / `getPageCount` appeared. Those helpers are only consumed by the sort paths at `main.lua:1317,1349`, which we do not touch. `2-series-indicator` keys off `bookinfo.series` truthiness alone and paints a gray edge tab, unrelated to any helper. `2-series-badge-numbered` formats its own `"#" .. series_index`, duplicating one line of `ptutil.formatSeries` logic but not conflicting with it. Left as-is deliberately: rewriting working patches to call new upstream helpers is churn, not compatibility work. One upstream behavior shift worth knowing: `ptutil.formatSeriesIndex` never returns nil, so upstream's own `if series_index ~= nil` guard in `paintTo` is always true and series index 0 is no longer suppressed the way it was at v3.7. That is upstream's business, not ours, but it means a book with index 0 now shows upstream's numbered box where it previously showed nothing.
- [x] 6.5 `2-pages-badge.lua` — check against upstream's new `ptutil.getPageCount` and the 3.8 page-count sort feature — VERIFIED, no change needed. `ptutil.getPageCount` (`ptutil.lua:807`) is a three-line wrapper around `BookInfoManager:getBookInfo(...).pages`, exactly what our patch already does inline; `bookinfo.pages` semantics are unchanged. The page-count sort feature consumes it only from `main.lua:1349`. Our filename-regex fallback still applies, and now covers one extra case: the new Kobo virtual-library path builds its bookinfo with `pages = nil`.
- [~] 6.6 Repair whatever 6.1-6.5 found broken, one patch per commit — NOTHING TO REPAIR. All 9 Tier 2 patches verified compatible without edits.

### Findings that pre-answer Tier 1

Read while chasing 6.4/6.5, recorded here so group 7 does not re-derive them:

- `bookinfomanager.lua` drifted by far less than assumed. The whole diff is: two `CoverBrowser` -> `ProjectTitle` comment renames (one of them the "schema line 72 change" task 7.1 was built around, so there is NO schema change); a new `loadKoboMetadata` prelude in `getBookInfo` that returns early only for `KOBO_VIRTUAL://` paths and otherwise falls through to the untouched original body; and a `.sdr` sidecar skip in `findFilesInDir`. The cache/DB layer itself is unchanged.
- `BOOKINFO_COLS_SET` is still declared at `bookinfomanager.lua:79` and still referenced inside `getBookInfo`, so `2-pt-perf.lua:161`'s `getUpValue` anchor still resolves and the column list is unchanged.
- `update`'s in-place mutation of `bookinfo.series` cannot poison `2-pt-perf`'s cache, because the patch returns `shallowCopy(cached)` (`2-pt-perf.lua:254`) rather than the cached table.
- New perf consideration for `2-pt-perf`: the new sort collates call `ptutil.getSeries` / `getPageCount` once per file, and each of those calls `getBookInfo`. When `use_custom_sorts` is on, sorting a folder now issues a `getBookInfo` per entry, which is precisely the workload section 1 of `2-pt-perf` caches.

## 7. Tier 1 verification (BookInfoManager layer)

Verified static-only against `_ref/` at `2026.07-v3.8.3` / `v2026.07.1`. This tier
was scoped as the risky one on the assumption that the `BookInfoManager` cache/DB
layer had changed. It had not. One code change came out of the tier (7.0); the rest
is confirmation.

- [x] 7.0 Remove the now-dead `ptutil.make_sql_safe` fallback in `2-pt-perf.lua` (`c6a62c4`) — DONE. Confirmed absent at v3.7 and present at v3.8.3 with byte-identical escaping, so the fallback branch was unreachable under the new pin. Rather than deleting the fallback and leaving a bare `ptutil.make_sql_safe` reference (which would turn a missing function into a nil-call crash), added `"make_sql_safe"` to the `patchFolderCovers` version-guard loop that already sits above it, so a missing function produces the same clean logged bail as the other 8 dependencies.
- [x] 7.1 Read the full `bookinfomanager.lua` v3.7 → v3.8.3 diff — DONE, and the task's premise was WRONG on two counts. The "schema line change at 72" is a `CoverBrowser` -> `ProjectTitle` comment rename; `BOOKINFO_DB_SCHEMA` is otherwise untouched, so there is no schema change. The "~70 inserted lines before `getBookInfo`" are a self-contained `loadKoboMetadata` helper for the new Kobo virtual-library integration. The change to `getBookInfo` itself is a 4-line prelude that calls it and returns early only when the path starts with `KOBO_VIRTUAL://`; for every ordinary path it returns nil and execution falls through to the untouched original body. The only other change in the file is a `.sdr` sidecar skip in `findFilesInDir`. The cache and DB layer is unchanged.
- [x] 7.2 Answer design.md's open question: did any of `2-pt-perf.lua`'s caching land upstream? — ANSWERED: no. Upstream added no caching; the inserted lines are Kobo metadata loading, unrelated to the blob-free SELECT or the two-generation in-memory cache. Note for the record that `2-pt-perf.lua` was ALREADY written against v3.8.3 before this change: its header names "PT's custom sort collates" and states "Kobo virtual-library paths pass through untouched", and `2-pt-perf.lua:184,249` already short-circuits the `KOBO_VIRTUAL://` prefix to the original `getBookInfo`. So the one upstream addition that could have collided with section 1 was already accounted for.
- [~] 7.3 Repair or delete the superseded parts of `2-pt-perf.lua` — NOTHING SUPERSEDED beyond 7.0. No rewrite, so no re-scope needed. Anchors re-confirmed: `BOOKINFO_COLS_SET` is still declared at `bookinfomanager.lua:79`, still an upvalue of `getBookInfo`, and its column order is unchanged, so the patch's `META_SELECT_SQL` and its hardcoded `for i = 13, 20` slice in `getDocProps` still line up. That slice is not a guess: upstream annotates the same indices in the column list (`"pages", -- 13`, `"description", -- 20`) and its own `BookInfoManager:getDocProps` uses the identical `for i = 13, 20`.
- [x] 7.4 Verify `2-rounded-folder-covers.lua` against the changed layer — VERIFIED, no change needed. Its version guard takes `userpatch.getUpValue(MosaicMenuItem.update, "BookInfoManager")`; `BookInfoManager` is referenced 8 times inside `MosaicMenuItem:update`, so the upvalue resolves. All 8 folder-cover helpers the patch and `2-pt-perf` depend on (`query_cover_paths`, `build_cover_images`, `getSubfolderCoverImages`, `getFolderCover`, `build_diagonal_stack`, `build_grid`, `get_thumbnail_size`, `findCover`) exist at v3.8.3 and none appear as changed in the `ptutil.lua` diff; the only addition among that family is `make_sql_safe`.
- [x] 7.5 Confirm the two Tier 1 patches do not conflict — CONFIRMED, no conflict. The premise that "both wrap the same `BookInfoManager` methods" is not accurate: `2-rounded-folder-covers` wraps neither `getBookInfo` nor any `ptutil` function (it wraps `MosaicMenuItem.update` and adds `_setFolderCover`), so there is no double-wrap of anything. Both ship a copy of `installInvalidationRegistry`, which is idempotent via `BookInfoManager._cache_registry_installed`: whichever patch loads first installs the registry, the other reuses it, and each appends its own listener to the shared `_cache_listeners`. That duplication is deliberate so either patch works standalone (`2-pt-perf.lua:62`). Upstream's in-place mutation of `bookinfo.series` in `MosaicMenuItem:update` cannot poison the metadata cache, because `2-pt-perf.lua:254` hands out `shallowCopy(cached)` rather than the cached table.

## 8. Sweep version references

- [x] 8.1 Update `docs/installation.md` and note PT's differing notation (`9888e40`) — the manual-install table now reads `2026.07.1` "Sailing Walrus" / `2026.07-v3.8.3`, followed by a note that KOReader tags this `v2026.07.1` while PT's release notes write it `2026.07.01`, and that this repo uses KOReader's form. `docs/plugins.md`'s compatibility table was bumped too; it was not in this task's list but carries the same claim.
- [x] 8.2 Update the "Written against" line in all 19 `docs/patches/*.md` (`9888e40`) — 19/19. `2-pt-footer-history-recent` also dropped its `commit 31c6777` ref for the tag, matching what `a3dc216` did to the `.lua` header, and `2-bookend-line-font-inherit` gained a KOReader version it never carried so it agrees with its own patch header. Separately, `3d3c139` corrected the `**Patches:**` target line in the 13 PT docs from `coverbrowser` to `projecttitle` and deleted the "works with stock coverbrowser and Project: Title" claim from 5 of them, which `e24797f` had made false.
- [x] 8.3 Regenerate `patches/README.md` with the `patch-changelog` skill (`a2ffd0b`) — Targets column derived from each patch's measured `registerPatchPluginFunc` key (13 `projecttitle` / 2 `bookends` / 2 `coverimage` / 2 `core`), so the old `coverbrowser` vs `coverbrowser (PT)` split is gone since it no longer distinguishes anything. The uniform KOReader version moved to a note above the table instead of repeating in 19 rows. Existing curated summaries were kept deliberately: raw first-line extraction yields noise on 8 files (`[[`, `[[ User patch for KOReader to add...`), and no patch was added, removed, or renamed.
- [x] 8.4 Confirm the survivor grep returns only intentional historical references — CONFIRMED. Survivors, all deliberate: `.github/scripts/tests/fixtures/*.yml` (the manifest tests assert on `2026.03`/`Snowflake`, so changing them would break the suite), this change's own artifacts (which describe the transition *from* 2026.03), `docs/superpowers/` (historical design specs dated 2026-05-11), and `.claude/skills/koreader-version-bump/SKILL.md:40` (an illustrative `202603000000` sample output, not a claim about repo state).

## 9. Corrections and close out

- [x] 9.0 Correct `design.md`'s pin-format decision — DONE. The decision is retitled "Pin format: prose header only" and carries a **Corrected during implementation** note recording the false "extends a convention already in the repo" claim, its cause (the audit skill's regex was matching comment prose, not a declaration), and why the literal was dropped (19 unused locals with `unused-local` diagnostics).
- [x] 9.0b Fix pre-existing British `optimised` in `2-coverimage-eink-optimize.lua:236` (`f80a19d`) — verified as the only British spelling remaining in `patches/` and `docs/`, excluding the historical `docs/superpowers/` specs.

- [x] 9.1 Record each patch's verification level per the spec's third requirement — DONE. Every `docs/patches/*.md` now carries a `**Verified:**` bullet directly under `**Written against:**`: 7 at symbol level, 12 re-checked against changed code, 19 of 19 total. Writing this up exposed a real gap and closed it: the 4 non-plugin patches (`2-menu-size`, `2-disable-input-rotation-map`, `2-coverimage-lighten`, `2-coverimage-eink-optimize`) had declared pins from `a3dc216` but had never been verified against 2026.07.1 at all, which under requirement 3 makes them declared-but-unverified rather than compatible. Verified at symbol level rather than recorded as a gap: `TouchMenu.max_per_page_default` (`touchmenu.lua:471`, still consulted at 661) and `Menu.items_per_page_default` (`menu.lua:613`, still consulted at 647); `Input.rotation_map` (`input.lua:163,276`); `CoverImage:createCoverImage` / `menuEntrySBF` / `coverEnabled` (`plugins/coverimage.koplugin/main.lua:123,664,245`) plus all four `cover_image_*` fields; `Screensaver:show` (`screensaver.lua:450`), `Device:supportsScreensaver` (`device.lua:566`), and `frontend/ui/elements/screensaver_menu.lua`.
- [x] 9.2 Note explicitly how verification was done — DONE, and amended by 9.3. All per-patch verification recorded in groups 5 to 7 and in task 9.1 is STATIC: tag-to-tag `git diff` of hooked function bodies, plus symbol and upvalue resolution against `_ref/` pinned to `v2026.07.1` / `2026.07-v3.8.3` / `v5.22.0`. No patch was exercised at runtime to produce those findings. One `_ref/` limitation worth recording: `setup-refs.sh` does not init submodules, so `base/` (koreader-base) is empty and `ffi/*` modules cannot be resolved there; `ffi/archiver`, which `2-bookend-unifont.lua:29` requires, was confirmed present instead via KOReader's own core use of it at `frontend/apps/reader/readerui.lua:7`.
- [x] 9.3 Manual smoke test on device before any release tag — DONE by the user, who sideloaded a locally built bundle and reported it working. This is the first and only runtime evidence for this change; everything else is static. Scope caveat, recorded so it is not over-read later: the report was a general "worked great" rather than an itemized per-patch pass, so it should be treated as strong evidence that nothing is broadly broken (notably that the 13 newly-reactivated PT patches load and do not error), not as per-patch runtime confirmation of each of the 19.
- [ ] 9.4 Run `openspec validate koreader-2026-07-patch-compat --strict` and archive the change
